# Security policy

Do not open public issues for suspected vulnerabilities or exposed secrets. Contact the repository maintainer privately through GitHub instead.

If you accidentally commit a secret, revoke or rotate it immediately. Removing a file in a later commit is not sufficient.
